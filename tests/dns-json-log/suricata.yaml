%YAML 1.1
---

include: ../../etc/suricata-4.0.3.yaml

# Configure the type of alert (and other) logging you would like.
outputs:

  - dns-json-log:
      enabled: yes
      filename: dns.json
      version: 2
