requires:
  features:
    - HAVE_LIBJANSSON
  min-version: 4.1.0

checks:

  - filter:
      count: 20
      match:
        dns.type: query

  - filter:
      count: 20
      match:
        dns.type: answer
