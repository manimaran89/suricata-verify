%YAML 1.1
---

include: ../../etc/suricata-4.0.3.yaml

# Remove stats logging.
stats:
  enabled: no

outputs:
  - eve-log:
      enabled: true
      types:
        - dns:
            enable: true
            version: 2
          
