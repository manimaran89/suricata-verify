requires:
  features:
    - HAVE_LIBJANSSON
  min-version: 4.1.0

checks:

  - filter:
      count: 2
      match:
        event_type: dns
        dns.type: query

  - filter:
      count: 2
      match:
        event_type: dns
        dns.type: answer
