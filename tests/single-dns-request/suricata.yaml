%YAML 1.1
---

include: ../../etc/suricata-4.0.3.yaml

outputs:
  - eve-log:
      enabled: yes
      filename: eve.json
      types:
        - dns:
            enabled: yes
            version: 2
        
