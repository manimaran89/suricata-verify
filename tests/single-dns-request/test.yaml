requires:
  features:
    - HAVE_LIBJANSSON

checks:
  - filter:
      count: 1
      match:
        event_type: dns
        dns.type: query
        dns.rrname: suricon.net
  - filter:
      count: 1
      match:
        event_type: dns
        dns.type: answer
        dns.answers[0].rdata: 181.224.138.142
        
